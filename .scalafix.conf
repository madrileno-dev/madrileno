rules = [
  DisableSyntax,
  ExplicitResultTypes,
  LeakingImplicitClassVal,
  NoAutoTupling,
  NoValInForComprehension,
  OrganizeImports,
  ProcedureSyntax,
  RedundantSyntax,
  RemoveUnused
]

# Scalafix ships ExplicitResultTypes compiled against one Scala 3 minor; when the
# project's Scala minor is newer, let it fetch matching compiler artifacts instead of failing.
ExplicitResultTypes.fetchScala3CompilerArtifactsOnVersionMismatch = true

DisableSyntax {
  noVars = true
  noNulls = true
  noReturns = true
  noWhileLoops = true
  noAsInstanceOf = true
  noIsInstanceOf = true
  noXml = true
  noFinalVal = true
  noFinalize = true
  noValPatterns = true
  regex = [
    {
      id = "noRandomUuid"
      pattern = "\\bUUID\\.randomUUID"
      message = "Use IdGenerator.generateId / UuidV7.generate for time-ordered UUIDv7 ids (in tests, TestData.randomUuid / random*Id). Don't generate random v4 UUIDs directly."
    }
  ]
}

OrganizeImports {
  targetDialect = Scala3
  blankLines = Auto
  coalesceToWildcardImportThreshold = null
  expandRelative = false
  groupedImports = Merge
  groups = [
    "*"
    "re:(javax?|scala)\\."
  ]
  importSelectorsOrder = Ascii
  importsOrder = Ascii
  preset = DEFAULT
  removeUnused = true
}